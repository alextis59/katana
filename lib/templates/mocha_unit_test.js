const sinon = require("sinon");
const chai = require('chai');
const sinonChai = require("sinon-chai");
chai.use(sinonChai);
const { expect, AssertionError } = require('chai');
[REQUIRE_LINES]

describe("[MODULE_NAME].[FUNCTION_NAME].[TEST_INDEX]", () => {

    // SETUP CODE

    it("[TEST_CASE]", async () => {
        
        // CODE HERE

    });

});